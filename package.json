{
  "name": "image-proxy-server",
  "version": "1.0.0",
  "description": "A proxy server using Puppeteer to handle image downloads.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "proxy",
    "cors",
    "render",
    "node",
    "express",
    "puppeteer"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "puppeteer": "^22.9.0"
  }
}
